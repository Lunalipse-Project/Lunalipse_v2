<Page x:Class="LunalipseInstaller.Pages.InstallProcess"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LunalipseInstaller.Pages"
      xmlns:root="clr-namespace:LunalipseInstaller"
      mc:Ignorable="d" 
      Height="150" Width="800"
      Title="InstallProcess">

    <Grid>
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <root:LpsProgressBar x:Name="InstallIndicator" Grid.Row="0" Height="15" Margin="150 20 150 0"/>
            <Label x:Name="Message"  Grid.Row="1" FontSize="20" Foreground="White" Content="Install Message" HorizontalAlignment="Center" Margin="0 20 0 0"/>
            <Label x:Name="DetailedMsg" Grid.Row="2" FontSize="12" Foreground="White" Content="Install Message" HorizontalAlignment="Center" Margin="0 -5 0 0" Opacity="0.6"/>
        </Grid>
    </Grid>
</Page>
